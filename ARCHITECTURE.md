# Wedding Planner - System Architecture

## 🏗️ Architecture Overview

```
┌─────────────────────────────────────────────────────────────┐
│                         CLIENT LAYER                         │
│                    (React Frontend - Port 3000)              │
│                                                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐     │
│  │   Login &    │  │  Dashboard   │  │    Vendor    │     │
│  │   Register   │  │    Pages     │  │   Booking    │     │
│  └──────────────┘  └──────────────┘  └──────────────┘     │
│                                                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐     │
│  │    Guest     │  │    Budget    │  │    Tasks &   │     │
│  │  Management  │  │   Tracking   │  │   Timeline   │     │
│  └──────────────┘  └──────────────┘  └──────────────┘     │
│                                                              │
│              Authentication Context (JWT)                    │
└──────────────────────────┬──────────────────────────────────┘
                           │ HTTP/HTTPS (Axios)
                           ▼
┌─────────────────────────────────────────────────────────────┐
│                      API LAYER                               │
│               (Express.js - Port 5000)                       │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐  │
│  │              Middleware Layer                         │  │
│  │  • CORS        • JWT Auth     • Error Handler        │  │
│  │  • Body Parser • Authorization • Request Logger      │  │
│  └──────────────────────────────────────────────────────┘  │
│                           │                                  │
│  ┌──────────────────────────────────────────────────────┐  │
│  │              API Routes                               │  │
│  │  /api/auth          /api/weddings                     │  │
│  │  /api/guests        /api/vendors                      │  │
│  │  /api/bookings      /api/budget                       │  │
│  │  /api/tasks         /api/timeline                     │  │
│  │  /api/admin                                           │  │
│  └──────────────────────────────────────────────────────┘  │
│                           │                                  │
│  ┌──────────────────────────────────────────────────────┐  │
│  │              Controllers                              │  │
│  │  • Auth Logic      • Business Logic                   │  │
│  │  • Validation      • Data Processing                  │  │
│  └──────────────────────────────────────────────────────┘  │
│                           │                                  │
│  ┌──────────────────────────────────────────────────────┐  │
│  │              Models (Sequelize ORM)                   │  │
│  │  User | Wedding | Guest | Vendor | Booking           │  │
│  │  BudgetItem | Task | TimelineEvent                   │  │
│  └──────────────────────────────────────────────────────┘  │
└──────────────────────────┬──────────────────────────────────┘
                           │ SQL Queries
                           ▼
┌─────────────────────────────────────────────────────────────┐
│                    DATABASE LAYER                            │
│                  (PostgreSQL - Port 5432)                    │
│                                                              │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐  │
│  │  Users   │  │ Weddings │  │  Guests  │  │ Vendors  │  │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘  │
│                                                              │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐  │
│  │ Bookings │  │  Budget  │  │  Tasks   │  │ Timeline │  │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘  │
└─────────────────────────────────────────────────────────────┘
```

## 🔄 Data Flow Diagram

### User Authentication Flow
```
User Input → Register/Login Page → Auth Service
                                        ↓
                                   POST /api/auth/login
                                        ↓
                              Auth Controller validates
                                        ↓
                               Check User in Database
                                        ↓
                          Generate JWT Token & Send Response
                                        ↓
                         Store Token in localStorage
                                        ↓
                          Redirect to Dashboard
```

### Wedding Creation Flow
```
User → Dashboard → Create Wedding Button
                          ↓
                   Fill Wedding Form
                          ↓
              POST /api/weddings (with JWT)
                          ↓
              Auth Middleware validates token
                          ↓
           Wedding Controller processes data
                          ↓
              Save to Database (Wedding table)
                          ↓
           Return success response
                          ↓
         Update UI with new wedding
```

### Guest RSVP Flow
```
Guest Data Entry → Guest Form
                        ↓
         POST /api/weddings/:id/guests
                        ↓
         Verify wedding ownership
                        ↓
          Create Guest in Database
                        ↓
      Update Wedding guest count
                        ↓
        Return guest data to frontend
                        ↓
         Update guest list UI
```

## 📊 Database Relationships

```
        ┌─────────┐
        │  Users  │
        └────┬────┘
             │ 1:N
             ▼
        ┌─────────┐
        │Weddings │◄──────────┐
        └────┬────┘           │
             │ 1:N            │
             ├────────────────┼─────────────┐
             │                │             │
             ▼                ▼             ▼
        ┌─────────┐     ┌─────────┐   ┌─────────┐
        │ Guests  │     │ Budget  │   │  Tasks  │
        └─────────┘     │  Items  │   └─────────┘
                        └─────────┘
             │                              │
             │ 1:N                      1:N │
             ▼                              ▼
        ┌─────────┐                  ┌──────────┐
        │Bookings │◄─────────────────│ Timeline │
        └────┬────┘          N:1     │  Events  │
             │                        └──────────┘
             │ N:1
             ▼
        ┌─────────┐
        │ Vendors │
        └─────────┘
```

## 🔐 Authentication Flow

```
┌──────────────┐
│   Browser    │
└──────┬───────┘
       │ 1. Login request (email, password)
       ▼
┌──────────────────┐
│  Auth Controller │
└──────┬───────────┘
       │ 2. Validate credentials
       ▼
┌──────────────────┐
│    Database      │
└──────┬───────────┘
       │ 3. Return user data
       ▼
┌──────────────────┐
│  Auth Controller │
└──────┬───────────┘
       │ 4. Generate JWT token
       │ 5. Return token + user data
       ▼
┌──────────────────┐
│     Browser      │
│  localStorage    │
└──────┬───────────┘
       │ 6. Store token
       │ 7. Include in future requests
       ▼
┌──────────────────┐
│ Protected Routes │
│ (Authorization:  │
│  Bearer <token>) │
└──────────────────┘
```

## 🛡️ Security Layers

```
┌─────────────────────────────────────┐
│      Client Side Security           │
│  • HTTPS Only (Production)          │
│  • Token stored in localStorage     │
│  • Private route protection         │
│  • Form validation                  │
└────────────┬────────────────────────┘
             │
             ▼
┌─────────────────────────────────────┐
│      Transport Security              │
│  • JWT Token in Authorization header│
│  • CORS configuration                │
│  • Request/Response encryption       │
└────────────┬────────────────────────┘
             │
             ▼
┌─────────────────────────────────────┐
│      Server Side Security            │
│  • JWT verification middleware       │
│  • Role-based authorization          │
│  • Input sanitization                │
│  • Error handling                    │
└────────────┬────────────────────────┘
             │
             ▼
┌─────────────────────────────────────┐
│      Database Security               │
│  • Password hashing (bcrypt)         │
│  • Parameterized queries (Sequelize) │
│  • Connection pooling                │
│  • SQL injection protection          │
└─────────────────────────────────────┘
```

## 📱 Component Hierarchy

```
App
├── AuthProvider (Context)
│   └── Router
│       ├── Public Routes
│       │   ├── Login
│       │   └── Register
│       │
│       └── Private Routes
│           ├── Navbar
│           ├── Dashboard
│           │   ├── WeddingCard
│           │   ├── StatsCard
│           │   └── CreateWeddingModal
│           │
│           ├── WeddingDetail
│           │   ├── WeddingInfo
│           │   ├── QuickActions
│           │   └── TabNavigation
│           │
│           ├── Guests
│           │   ├── GuestList
│           │   ├── GuestForm
│           │   └── RSVPStats
│           │
│           ├── Vendors
│           │   ├── VendorCard
│           │   ├── VendorFilter
│           │   └── BookingModal
│           │
│           ├── Budget
│           │   ├── BudgetChart
│           │   ├── BudgetList
│           │   └── AddBudgetItem
│           │
│           ├── Tasks
│           │   ├── TaskList
│           │   ├── TaskForm
│           │   └── TaskFilters
│           │
│           ├── Timeline
│           │   ├── TimelineView
│           │   ├── EventCard
│           │   └── AddEventForm
│           │
│           └── AdminDashboard
│               ├── UserManagement
│               ├── VendorManagement
│               └── Statistics
```

## 🔄 State Management

```
┌─────────────────────────────────────┐
│      React Context API              │
│                                      │
│  AuthContext                         │
│  ├── user (current user)             │
│  ├── token (JWT)                     │
│  ├── login()                         │
│  ├── logout()                        │
│  ├── isAuthenticated                │
│  └── isAdmin                         │
│                                      │
│  Component State (useState)          │
│  ├── Form data                       │
│  ├── Loading states                  │
│  ├── Error messages                  │
│  └── UI state                        │
│                                      │
│  API Responses (useEffect)           │
│  ├── Wedding data                    │
│  ├── Guest lists                     │
│  ├── Vendor info                     │
│  └── Statistics                      │
└─────────────────────────────────────┘
```

## 📦 Deployment Architecture

```
┌─────────────────────────────────────┐
│     Frontend (Vercel/Netlify)       │
│     • React Build                    │
│     • Static Assets                  │
│     • CDN Distribution               │
└────────────┬────────────────────────┘
             │ HTTPS
             ▼
┌─────────────────────────────────────┐
│     Backend (Heroku/Railway)        │
│     • Node.js Server                 │
│     • Express API                    │
│     • Environment Variables          │
└────────────┬────────────────────────┘
             │ PostgreSQL Protocol
             ▼
┌─────────────────────────────────────┐
│     Database (Heroku Postgres)      │
│     • PostgreSQL Instance            │
│     • Automated Backups              │
│     • Connection Pooling             │
└─────────────────────────────────────┘
```

## 🚀 Request/Response Cycle

```
1. User Action (Click button)
        ↓
2. React Event Handler
        ↓
3. API Service Call (Axios)
        ↓
4. HTTP Request with JWT
        ↓
5. Backend Middleware (CORS, Auth)
        ↓
6. Route Handler
        ↓
7. Controller Logic
        ↓
8. Database Query (Sequelize)
        ↓
9. Database Response
        ↓
10. Controller formats response
        ↓
11. JSON Response to client
        ↓
12. Axios resolves promise
        ↓
13. React updates state
        ↓
14. UI re-renders
```

## 💾 Data Storage

```
┌─────────────────────────────────────┐
│         Client Storage               │
│  localStorage:                       │
│  • JWT token                         │
│  • User data                         │
│                                      │
│  sessionStorage:                     │
│  • Temporary form data               │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│         Server Storage               │
│  Environment Variables:              │
│  • Database credentials              │
│  • JWT secret                        │
│  • API keys                          │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│      Database Storage                │
│  Tables:                             │
│  • Users, Weddings, Guests           │
│  • Vendors, Bookings                 │
│  • BudgetItems, Tasks                │
│  • TimelineEvents                    │
└─────────────────────────────────────┘
```

---

**This architecture ensures scalability, security, and maintainability! 🏗️**
